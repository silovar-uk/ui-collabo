# UI改善計画 — 「使い方がわかる」「触っていて楽しい」へ

- 作成日: 2026-09-07
- 作成: Claude Opus 5(計画のみ。コードは変更していません)
- 実装担当: Claude Sonnet(この文書を読んで実装する)
- 対象: `silovar-uk/ui-collabo` v0.1.0(https://silovar-uk.github.io/ui-collabo/ にデプロイ済み)
- 前提文書: [PLAN.md](PLAN.md)(初期計画)、[docs/SPEC.md](docs/SPEC.md)、[docs/DECISIONS.md](docs/DECISIONS.md)

---

## 0. Sonnetへ: この文書の読み方

- 第1章が診断です。**なぜ直すのか**が書いてあります。実装前に必ず読んでください
- 第3章がフェーズです。**A→B→C→D→E の順に実装**してください。各フェーズの受け入れ基準を満たしてから次へ進みます
- フェーズAとBだけで体験の大半が変わります。時間が足りなければ、CとDを削るよりも**Aを完全にやりきってください**
- 第4章に「やらないこと」があります。ここに書いてあることは実装しないでください
- 機能は足しません。**今ある機能の見せ方だけを変えます**。新しいノート種別・新しいピッカー・新しい依存パッケージは追加しません
- [PLAN.md](PLAN.md) 第10章の原則(足すより減らす、選択肢は実物で見せる、言葉は会話語、など)は引き続き有効です
- 判断で埋めた穴は、これまでどおり [docs/DECISIONS.md](docs/DECISIONS.md) に1行ずつ残してください

---

## 1. 診断

公開版を実際に操作し、ソースと初期計画([PLAN.md](PLAN.md))を突き合わせた結果です。

### 1.1 わかりづらさの正体

**結論: 操作しても結果が見えないため、何をしているのか最後までわからない状態が続きます。**

初期計画([PLAN.md](PLAN.md) 第2章)は、このアプリで最も重要な体験をこう定義していました。

> **「指したら、その場で言葉になる」**
> 出力は最後に押す「書き出しボタン」ではなく、**常に見えているもの** として設計します。

しかし現在の実装では、指示文は「AIに渡す」ドロワーを開かないと一切見えません。
チップを押しても画面には何の変化も起きず、ユーザーは「今の操作が何になったのか」を
確認できないまま次の操作に進みます。これが分かりにくさの根本原因です。

そのうえに、以下が重なっています。

| # | 症状 | 場所 |
|---|---|---|
| 1 | 初期画面に、アプリの説明・ロゴ・タグラインが一切ない。「貼る」「白紙から」「開く」という3語だけが並ぶ | `src/panels/Empty.tsx` |
| 2 | 「貼る」は説明文だけで、ドロップ領域の見た目がない。**クリックでファイルを選ぶ手段が存在しない**(Ctrl+Vかドラッグのみ) | `src/app.tsx` / `Empty.tsx` |
| 3 | 初見が最初に触るべき「サンプル」が、右端カラムの最下部にある | `src/panels/Empty.tsx` |
| 4 | ボードを開いても「ドラッグで囲む」というヒントがない。白紙は斜線ハッチが表示されるだけ | `src/board/Board.tsx` |
| 5 | 箇所を1つ選ぶと、右パネルに**40個以上のコントロールが同じ見た目で縦に並ぶ**。優先順位がない | `src/panels/SpotPanel.tsx` |
| 6 | 色を1つ足すと、方向9種+ルール色4種+hex入力+役割4種+昇格+削除の**約20個が一度に開く** | `src/pickers/ColorPicker.tsx` |
| 7 | 「ルールにする」ボタンが各所に散在するが、説明がない。ルールを作っていない初回ユーザーには意味不明 | `SpotPanel.tsx` 全域 |
| 8 | 「量(文字サイズ・余白・角丸など)」という抽象的な見出しの下に、7つの「＋」ボタンが常時展開されている | `SpotPanel.tsx` |
| 9 | チップ(トグル)とボタン(実行)が同じ形・同じ色で、押した後に何が起きるか予測できない | `src/styles.css` |
| 10 | 箇所のラベルが黒帯で箱の**外側上部**に出るため、画像の文字と重なって読めない | `src/board/SpotRect.tsx` / `styles.css` |
| 11 | 番号バッジが箱の外に `-10px` はみ出すため、ボード端の箇所では `overflow:hidden` に切られて消える | `styles.css` `.spot-badge` |
| 12 | ゴールである「AIに渡す」への進捗が見えない。何個指定できたのかフィードバックがない | `src/app.tsx` |

### 1.2 つまらなさの正体

**結論: ブランドの視覚言語が画面のどこにも現れておらず、実質モノクロの管理画面になっています。**

[PLAN.md](PLAN.md) 4.2 が定めたシンボルの意味は明快です。

> 墨色の塗り四角 = **今の状態** / 朱色の点線四角 = **こうしたい状態**
> 朱色は日本の校正の「赤入れ」に由来します。デザインレビューは赤入れです。

ところが実装では、朱色(`--vermilion`)は**リサイズハンドルとページサムネの枠にしか使われていません**。
最も重要な「こうしたいを選んだ状態」(アクティブなチップ)は墨色で塗られており、
ブランドが定義した意味と逆になっています。

| # | 症状 | 場所 |
|---|---|---|
| 13 | 文字サイズが 12/13/14/15/16px の5段階しかなく、見出しが15px。階層が平坦で目の休まる場所がない | `src/styles.css` |
| 14 | アクティブなチップが墨色。**朱色=こうしたい、というブランドの定義と矛盾**している | `styles.css` `.chip.is-active` |
| 15 | ドロワーは「右からスライドイン」と設計されているが、CSSに `transition` がなく実際は瞬間表示 | `styles.css` `.drawer` |
| 16 | ロゴマークが画面のどこにも表示されていない(`brand/mark.svg` は存在するのに未使用) | `src/app.tsx` |
| 17 | `.spot-rect.is-selected` というクラスをJSXが出力しているのに、**CSSに定義がない**。選択状態が箱自体には現れない | `SpotRect.tsx` / `styles.css` |
| 18 | ホバーの反応が `opacity: 0.85` か枠色変更のみ。影・フォーカスリングがなく、面が沈んで見える | `styles.css` |
| 19 | 補助文字色 `--muted` (#8A857C) は白背景で**コントラスト比 約3.6:1**。12pxの本文サイズではWCAG AA(4.5:1)に届かない | `styles.css` |

### 1.3 初期計画から落ちているもの

現在の実装は [PLAN.md](PLAN.md) の設計から、次の4点が欠けた状態です。
**この改善は「新しいUIを考える」のではなく、元の設計に戻す作業が中心です。**

| 落ちているもの | 出典 | 対応フェーズ |
|---|---|---|
| 指示文プレビューの常時表示(右パネル下部、折りたたみ可) | 5.2 画面構成図 | **A** |
| 「＋ こうしたい」という単一の入口から種類を選ぶ操作 | 5.4 基本ループ | **C** |
| ワードマークにロゴマークを併記する | 4.3 | **D** |
| ドロワーのスライドイン | 5.2 | **D** |

---

## 2. 方針

1. **機能は足さない。見せ方だけを変える。** できることは十分あります。伝わっていないだけです
2. **段階開示。** 初回に見えるものを減らし、必要になった時点で開きます
3. **すべての操作に、その場の手応えを返す。** 押した結果が指示文のどの行になったかを見せます
4. **朱=こうしたい、墨=今、を全画面で徹底する。** ブランドの意味とUIの意味を一致させます
5. **データ構造は触らない。** `src/schema.ts` と保存形式は変更しません。既存の保存データが壊れないようにします

---

## 3. フェーズ

### フェーズA: 指示文プレビューを常時表示に戻す(最優先)

**なぜ:** 1.1 の根本原因を直します。これ1つで「何をしているのかわからない」がほぼ解消します。

**触るファイル**
- 新規 `src/panels/LivePreview.tsx`
- `src/app.tsx`(右パネルの構造)
- `src/styles.css`

**やること**

**A-1(必須)** 右パネルを上下2段に分け、下段に指示文プレビューを常時表示します。

- `.side-panel` を `display: grid; grid-template-rows: 1fr auto;` にします
- 上段は現在の内容(BoardPanel / SpotPanel)。`overflow: auto`
- 下段が `LivePreview`。`max-height: 40%`、内部スクロール、上辺に1px罫線
- 中身は `boardToMarkdown(board)`(`src/export.ts` に既存)の結果を `<pre>` で表示します
- 見出しは「いま、こう伝わります」。会話語にします([PLAN.md](PLAN.md) 10-6)
- 折りたたみはネイティブの `<details open>` を使います。JavaScriptでの開閉実装は不要です
- プレビュー右上に「コピー」ボタンを置きます。ドロワーを開かずに完結できるようにします

**A-2(推奨)** 選択中の箇所に対応する行を強調します。

- `boardToMarkdown` の出力を行分割し、`### {n} ` で始まる行から次の `###` 直前までを1ブロックとして判定します
- 選択中スポットの `n` と一致するブロックに `class="is-current"` を付け、左に朱色の縦線(`border-left: 2px solid var(--vermilion)`)と淡い背景を敷きます
- 選択が変わったらそのブロックへ `scrollIntoView({ block: 'nearest' })` します
- `export.ts` の出力フォーマットは変更しません。表示側で行を切り分けるだけにします

**A-3(必須)** ボードに箇所が0個のとき、プレビュー欄には出力の代わりに次の一文を出します。

> 画像の上をドラッグして、気になる箇所を囲んでください。

**受け入れ基準**
- サンプルボードを開き、チップを1つ押すと、**右パネル下部の文章がその場で変わる**
- 箇所を選び直すと、対応するブロックが朱色の線で示される(A-2実装時)
- 「コピー」で指示文全文がクリップボードに入る
- 右パネルを縮めてもプレビューが消えない(常に画面内に残る)

---

### フェーズB: 入口を作り直す

**なぜ:** 1.1 の #1〜#3 を直します。初見が「何のアプリで、まず何をすればいいか」を5秒で理解できるようにします。

**触るファイル**
- `src/panels/Empty.tsx`(全面書き換え)
- 新規 `src/lib/intake.ts`(画像取り込み処理の切り出し)
- `src/app.tsx`(`handleFiles` を `intake.ts` へ移す)
- `src/styles.css`

**やること**

**B-1** `app.tsx` の `handleFiles` / `filesToPages` を `src/lib/intake.ts` へ移します。
`Empty.tsx` からも呼べるようにするためです。ロジックは変更しません。

**B-2** `Empty.tsx` を次の構成に組み替えます。

```
        [mark.svg 40px]  UI ColLabo
        「なんか違う」を、伝わる形に。

  ┌──────────────────────────┐   ┌────────────────────┐
  │                          │   │ まず触ってみる        │
  │   スクリーンショットを      │   │  ・サンプル: Webページ │
  │   ここにドロップ           │   │  ・サンプル: スライド  │
  │                          │   └────────────────────┘
  │   Ctrl+V で貼り付け        │
  │   クリックしてファイルを選ぶ │
  └──────────────────────────┘

  ① 貼る  →  ② 囲う  →  ③ 選ぶ  →  ④ 渡す
  画像を    気になる     こうしたいを   指示文をコピーして
  貼る      箇所を囲む   選ぶ          AIへ

  ▸ 白紙から始める(Web / スライド 16:9 / スライド 4:3)
  ▸ 保存したボード(n件)   ← 0件のときは非表示
```

- 上段のドロップゾーンは朱色の点線枠(`2px dashed var(--vermilion)`)、最小高さ 220px
- **クリックでファイル選択できるようにします**。`<label>` + `<input type="file" accept="image/*" multiple hidden>` を使い、`onChange` で `intake.handleFiles` を呼びます
- 4ステップは装飾ではなく説明です。数字・動詞・一言の3要素をCSS Gridで横並びにします。SVGの図解は作りません
- 「白紙から」「保存したボード」はネイティブの `<details>` で畳みます。既定は閉じた状態です
- 保存ボードが1件以上あるときは「保存したボード」の `<details>` を開いた状態で表示します(リピーターの導線)

**B-3** `.app-shell.is-drag-over` の見せ方を強くします。
現在は3px破線のアウトラインのみです。ドラッグ中はドロップゾーンの背景を朱色 8% で塗り、
「ここに落とす」の文言に切り替えます。

**受け入れ基準**
- 初回訪問時、画面を見ただけで「スクリーンショットを貼るアプリ」だと分かる
- **マウスだけで**(Ctrl+Vもドラッグも使わずに)画像を読み込める
- サンプルが上段の右半分にあり、1クリックで開ける
- 保存ボードが0件のとき、画面に余計な空リストが出ない

---

### フェーズC: 右パネルを段階開示にする

**なぜ:** 1.1 の #5〜#9 を直します。1画面のコントロールを40個から10個程度に減らします。

**触るファイル**
- `src/panels/SpotPanel.tsx`
- `src/pickers/ColorPicker.tsx`
- `src/styles.css`

**やること**

**C-1(必須)** 「量」の7つの「＋」ボタンを、1つの「＋ 量を指定する」に畳みます。

- 押すと属性の選択肢(文字サイズ / 文字の太さ / 余白 / 角丸 / 全体の大きさ / 線の太さ / 動きの速さ / 動きの強さ)がその場に出ます
- 属性を選ぶと、現在と同じくラダーが開きます
- 既存の `adder` ステートの仕組みをそのまま使えます。新しい状態管理は作りません
- 見出しの文言を「量(文字サイズ・余白・角丸など)」から「**大きさ・余白・形**」に変えます([PLAN.md](PLAN.md) 10-6: 言葉は会話語)

**C-2(必須)** 色ノートを、閉じた状態を既定にします。

- 閉じているときの表示は1行だけにします: `[スウォッチ] #3266cc → #c94a1d(落ち着かせる)` + 「編集」「×」
- 「編集」を押したときだけ、現在の `ColorPicker` の中身を開きます
- 新規追加時は開いた状態で始めます(追加した直後に閉じていると操作できないため)

**C-3(必須)** 「ルールにする」を初期表示から隠します。

- 各ノートの中に `<details><summary>詳しく</summary>` を置き、その中へ移します
- `summary` の文言は「ルールにする」ではなく「**このボードの基準にする**」にします。「ルール」という語は、まだルールを1つも作っていない人には意味が伝わりません
- ルールが1つ以上あるボードでは `<details>` を開いた状態で出します

**C-4(推奨・余力があれば)** 箇所に付いている指定の一覧を、パネル上部にまとめます。

```
箇所 2  [CTAボタン        ]     [残す ▢]  [削除]

いま指定していること
 ● 色      #3266cc → #c94a1d      [編集] [×]
 ● 文字サイズ 少し小さく             [編集] [×]

＋ こうしたい  ← 押すと [位置・大きさ][色][文字][大きさ・余白・形][動き][ひとこと]
```

これは [PLAN.md](PLAN.md) 5.4 の「＋ こうしたい」から種類を選ぶ設計そのものです。
C-1〜C-3 を終えてから着手し、間に合わなければ次回に回してください。

**受け入れ基準**
- 箇所を新規作成した直後、右パネルの上段に見えるボタン・チップが**15個以下**である(現状は40個以上)
- 色を1つ足しても、パネルの見た目が1行分しか伸びない
- 「ルールにする」が初期表示に出てこない
- 既存の操作は1手増えるだけで、到達できなくなるものがない

---

### フェーズD: 見た目を「墨と朱」に寄せる

**なぜ:** 1.2 を直します。ブランドが定義済みの意味を、そのままUIに載せます。

**触るファイル**
- `src/styles.css`(中心)
- `src/app.tsx`(トップバーのマーク)

**やること**

**D-1** タイポと余白のスケールを `:root` に定義し、既存のバラバラな値を置き換えます。

```css
--fs-xs: 11px;   /* 補助ラベル */
--fs-sm: 13px;   /* チップ・小ボタン */
--fs-md: 15px;   /* 本文・入力 */
--fs-lg: 20px;   /* パネル見出し */
--fs-xl: 28px;   /* 入口のタイトル */
--sp-1: 4px; --sp-2: 8px; --sp-3: 12px; --sp-4: 16px; --sp-5: 24px; --sp-6: 32px;
```

**D-2** 朱色の意味を統一します。**ここが最も効きます。**

| 対象 | 現在 | 変更後 |
|---|---|---|
| `.chip.is-active`(選んだ「こうしたい」) | 墨色の塗り | **朱色の塗り** |
| `.ladder-thumb.is-target` | 朱の枠2px | 朱の塗り(選択済みが遠目で分かる) |
| `.spot-rect.is-selected` | **CSS定義なし** | 朱色の実線2px + `box-shadow: 0 0 0 3px rgba(228,87,46,.15)` を追加 |
| `.spot-badge` | 墨色 | 選択中のみ朱色 |
| `:focus-visible` | 未定義 | `outline: 2px solid var(--vermilion); outline-offset: 2px` を全対話要素に |

墨色は「今の状態」、朱色は「こうしたい/選んだもの」に固定します。
`.btn`(AIに渡す等の実行ボタン)は墨色のままにします。ここは「操作」であって「こうしたい」ではありません。

**D-3** 動きを足します。すべて `prefers-reduced-motion: reduce` で無効化します。

- ドロワー: `transform: translateX(100%)` からのスライドイン 180ms `ease-out`、backdropは opacity フェード
- チップ・ボタン: `transition: background-color 120ms, border-color 120ms`
- 箇所を新規作成したとき、その箱に 200ms の淡いパルス(`box-shadow` のみ。レイアウトは動かさない)

**D-4** ロゴマークをトップバーに出します。
`brand/mark-small.svg` を `public/` へコピーし、ワードマークの左に 20px で置きます。
(ビルド対象に含めるため、`src/` から `import` するか `public/` 配下に置くかは実装時に判断してください)

**D-5** 面に奥行きを出します。

- `.side-panel` と `.drawer` に `box-shadow: -1px 0 0 var(--line)` 相当の境界を保ちつつ、ドロワーには `-8px 0 24px rgba(28,27,25,.10)` を足します
- `.board-surface` に `box-shadow: 0 1px 3px rgba(28,27,25,.08)` を足し、和紙色の背景から浮かせます

**受け入れ基準**
- チップを選ぶと朱色になり、ロゴのシンボル(朱の点線四角)と意味がつながる
- キーボードのTabキーだけで操作でき、フォーカス位置が常に見える
- OSの「視差を減らす」設定でアニメーションが止まる
- ドロワーが右から滑って出る

---

### フェーズE: 詰め(キャンバスとアクセシビリティ)

**なぜ:** 1.1 の #10〜#12 と 1.2 の #19 を直します。細かいですが、放置すると「雑に作られている」印象が残ります。

**触るファイル**
- `src/board/SpotRect.tsx`、`src/panels/Drawer.tsx`、`src/app.tsx`、`src/styles.css`

**やること**

**E-1** 箇所のラベルと番号バッジの位置を直します。

- `.spot-badge` を箱の**内側**左上(`top: 2px; left: 2px`)に移します。ボード端でも切れなくなります
- `.spot-label` を箱の外側上部から**内側上部**へ移し、背景を `rgba(28,27,25,.82)` にして、`max-width: 100%` + `text-overflow: ellipsis` で溢れを止めます
- 箱の高さが 28px 未満のときはラベルを隠し、ホバー時のみ出します(`title` 属性にも入れておきます)

**E-2** ドロワーのアクセシビリティを整えます。

- `role="dialog" aria-modal="true" aria-label="..."` を `.drawer` に付けます
- **Escapeキーで閉じられるようにします**(現在はできません)
- 開いたときに最初のタブへフォーカスを移します。フォーカストラップの実装までは不要です

**E-3** トグルの状態を支援技術に伝えます。
チップ(`.chip`)は `aria-pressed={active}` を付けます。対象は `BoardPanel` のトーンチップ、
`SpotPanel` のトーンチップとルール参照、`ColorPicker` の方向チップです。

**E-4** 補助文字のコントラストを確保します。
`--muted` (#8A857C, 約3.6:1) は装飾・罫線用途に残し、**テキスト用に `--muted-text: #6E695F`(約5.5:1)を追加**して
`.field-label` `.muted` `.font-card-label` `.motion-label` などの文字色に使います。
[PLAN.md](PLAN.md) 4.3 のブランド定義表は変更しません。

**E-5** ゴールへの進捗を出します。
`.topbar` の「AIに渡す」ボタンに、指定済みノート数のバッジを出します。

```
[AIに渡す ③]   ← 箇所3つに何か指定がある状態
```

- 数え方は「1つ以上ノートを持つ箇所」または「残す」が付いた箇所の数です
- 0個のときはバッジを出しません

**受け入れ基準**
- ボード左上・右上の端に作った箇所でも、番号バッジが欠けない
- ラベルが画像の文字を隠さない
- ドロワーがEscで閉じる
- 指定を1つ足すと「AIに渡す」の数字が増える

---

## 4. やらないこと

以下は今回の範囲外です。実装しないでください。

| やらないこと | 理由 |
|---|---|
| ダークモード | [PLAN.md](PLAN.md) 3章で対象外と決定済み |
| モバイル・タブレット対応 | 同上(v1はデスクトップ前提) |
| 新しい依存パッケージの追加 | preact + signals のみで足ります。UIライブラリもアイコンフォントも入れません |
| `src/schema.ts` とデータ形式の変更 | 既存の保存データ(IndexedDB・書き出し済みJSON)が壊れます |
| `src/export.ts` の出力フォーマット変更 | フェーズAは表示側で行を切り分けるだけにします |
| 新しいノート種別・新しいピッカー | 機能は足りています |
| オンボーディングのツアー・モーダル・チュートリアル | 段階開示で解決します。初回モーダルは追加しません |
| ボードの複数同時表示、バージョン履歴、矢印注釈 | [PLAN.md](PLAN.md) 3章で対象外と決定済み |

---

## 5. 検証

各フェーズの完了時に実行してください。

```bash
npm run build
```

```bash
npm test
```

- `npm run build`(`tsc -b && vite build`)が警告なしで通ること
- `npm test`(`test/color.test.ts`, `test/export.test.ts`)が通ること。テストの変更が必要になった場合は、
  出力フォーマットを変えていないか第4章と照らしてください
- `npm run dev` で以下を目視確認します
  1. 初回画面 → マウスだけでサンプルを開ける
  2. 箇所を1つ作り、チップを1つ押す → 右パネル下部の文章がその場で変わる
  3. 箇所をボードの四隅に作る → 番号バッジが欠けない
  4. Tabキーだけで一巡できる → フォーカスが常に見える
  5. ドロワーを開いてEscで閉じる

実装完了後、`docs/screenshot.png` に実機のスクリーンショットを保存し、
README冒頭の画像を復活させてください([docs/DECISIONS.md](docs/DECISIONS.md) Phase 4 に残された宿題です)。

---

## 6. 優先度のまとめ

| フェーズ | 効果 | 規模 | 判断 |
|---|---|---|---|
| **A** 指示文プレビュー常時表示 | 特大 | 小(新規1ファイル + CSS) | **必ずやる** |
| **B** 入口の作り直し | 大 | 中(1ファイル書き換え) | **必ずやる** |
| **C** 右パネルの段階開示 | 大 | 中〜大 | C-1〜C-3は実施。C-4は余力次第 |
| **D** 墨と朱の徹底 | 中〜大 | 小(ほぼCSS) | **やる。費用対効果が最も高い** |
| **E** 詰め | 中 | 小 | やる。E-1とE-4は品質の下限として必須 |

AとDだけでも印象は大きく変わります。時間が限られる場合は **A → D → B → E → C** の順でも構いません。
