# 守破離計画 — 測って、見せて、確かめる赤入れへ

- 作成日: 2026-09-11
- 作成: Claude Opus 5(計画のみ。コードは変更していません)
- 実装担当: Claude Sonnet(この文書を読んで実装する)
- 対象: `silovar-uk/ui-collabo`(公開版 https://silovar-uk.github.io/ui-collabo/ と、手元の`feat/html-page`)
- 前提文書: [PLAN.md](PLAN.md)、[PLAN-UI.md](PLAN-UI.md)、[PLAN-HTML.md](PLAN-HTML.md)、[PLAN-LADDER.md](PLAN-LADDER.md)、[docs/UI-LANGUAGE.md](docs/UI-LANGUAGE.md)、[docs/DECISIONS.md](docs/DECISIONS.md)

---

## 0. Sonnetへ: この文書の読み方

- 第1章が結論、第2章が実機とコードで確かめた診断です。**なぜ直すのか**はここにあります
- 第3章が「守破離」の定義と判定基準です。迷ったらここに戻ってください
- 第4章(フェーズ0)で土台を1本にしてから、守(第5章)→破(第6章)→離(第7章)の順に進みます
- 各項目に「触るファイル」「やること」「受け入れ基準」があります。基準を満たしてから次へ進みます
- 第8章にユーザーの回答(Q1〜Q3)を記録してあります。**第8章と本文が食い違う場合は第8章を優先**してください
- 新しい本番依存パッケージは追加しません。`SCHEMA`は`ui-collabo/1`のままです。データの追加は省略可能なフィールドだけにします
- 判断で埋めた穴は、これまでどおり[docs/DECISIONS.md](docs/DECISIONS.md)に1行ずつ残してください
- push・PR作成・`main`への統合は、ユーザーの承認を得てから行います

---

## 1. 結論

このアプリの約束は、[PLAN.md](PLAN.md) 第2章の「**指したら、その場で言葉になる**」です。
いまは「言葉」にはなります。しかし「その場」では何も起きません。

- **守**: 初見の数秒で壊れて見える箇所を直します(枠のズレ、縦長画像の縮小、効かないCSS反映)。道具として当たり前の型(何度でも取り消せる、「今」が出力に届く)を揃えます
- **破**: 借り物の型を破ります(右側の設定フォーム、文字だけの手応え、折り返す段階表)。基準は「目線を箇所から動かさない」「結果をスクショの上で見せる」の2つです
- **離**: 「赤入れ」という比喩を、飾りではなく操作原理にします。校正者の道具で**今を測り**、直った版と**照合し**、**校正紙**で渡します

### 1.1 驚きの瞬間

驚きは「品よく静かに」起こします。和紙・墨・朱の3色の中だけで起こし、`prefers-reduced-motion`では止めます。音は出しません。

| # | 瞬間 | 項目 |
|---|---|---|
| 1 | 「少し小さく」を押すと、スクショの見出しがその場で縮む。青いボタンが朱に変わり、ポンと跳ねる | H1 透かし |
| 2 | 物差しの墨▲(今)と朱▼(こうしたい)の隙間が、ロゴの「ズレ」そのものになっている | H2 物差し |
| 3 | 指示文の行に触れると、朱の引き出し線が画面を横切って箇所を指す | H3 指示書 |
| 4 | スクショに定規を当てると「約24px」の寸法線が出て、今の文字サイズが決まる | R1 測る |
| 5 | HTMLを取り込むと「文字サイズ 11種類」。なんか違うの正体が数で見える | R1 測る |
| 6 | 直った版を貼ると前回の赤が浮かび「直った?」。全部○なら「校了」の判子が押される | R2 照合 |
| 7 | 書き出した画像が校正紙になっている。画像1枚で人にもAIにも伝わる | R3 校正紙 |

---

## 2. 診断(実機とコードで確かめたこと)

公開版を1440×900で操作し、手元の`feat/html-page`(e078f92)のソースと突き合わせた結果です。

### 2.1 土台が2本ある

| 場所 | 中身 |
|---|---|
| `origin/main`(c910482、公開中) | PR #1でマージした**フェーズA〜D**の実装(f9b36a1)。ブックマークレットなし |
| 手元`feat/html-page`(e078f92) | fa62a62から**別に**実装した**フェーズA〜E**(83ee51f)と、ラダーの実測現在地(e078f92) |

2本は同じ計画を独立に実装したもので、共通の祖先はfa62a62です。
手元の方が後に作られ、機能も多い(ブックマークレット、`autoNow`、UI-LANGUAGE.md)ため、手元を正とします(第4章)。

### 2.2 初見で壊れて見える

| # | 症状(実機) | 原因(コード) |
|---|---|---|
| 1 | サンプルを開いた直後、箇所の枠が画像と大きくずれる。見出しの枠が本文の位置に、ヒーロー画像の枠が画像の外に出る。何かクリックすると直り、ウィンドウ幅を変えるとまたずれる | `Board.tsx`が描画中に`containerRef.current`の寸法を読む。初回はrefが空で仮の1000×1000になり、リサイズでは再描画されない。`HtmlBoard.tsx`も同じ |
| 2 | 1280×1600のWebスクショが横長の枠(912×795)に縮小され、左右が空く | `.board-column`(flex縦並び)の中で`.board-surface`が縮み、`aspect-ratio`が崩れる。`flex-shrink: 0`を足すと897×1121に戻ることを実機で確認済み |
| 3 | HTMLページで「少し小さく」を選んでも見出しが縮まない(`.hero h1{font-size:56px}`のページ) | `#uic-preview`の`h1 { font-size: 40px; }`が詳細度で負ける。手元版の「`</head>`直前に置く」対策は同じ詳細度でしか勝てず、`#main h1`や`.a .b h1`には負ける |
| 4 | HTMLページで要素をクリックすると、番号枠が要素より上に小さく出る(公開版) | 比率を既定の高さ800で割っている。手元版では再現しない可能性があるため、統合後に再確認する |

### 2.3 手応えが文字にしか返らない

画像ページでは、何を選んでも**スクショ上には何も起きません**。
変わるのは右下の指示文だけで、ユーザーは結果を頭の中で想像するしかありません。
「動き」のピッカーでさえ、再生されるのは小さな箱の中の「あ」であり、自分の画面ではありません。
[docs/UI-LANGUAGE.md](docs/UI-LANGUAGE.md) の破れ#6「その場反映がHTMLページにしかない」は、ここが根です。

### 2.4 右パネルが長いフォームになっている

見出しの箇所を選んだ状態で、右パネル上段の対話要素は**53個**、縦の長さは**表示領域の2.7倍**です(実測)。
ユーザーの目線は、左のボードと右のフォームを往復し続けます。
指示文プレビューは下段の40vhに押し込まれ、全体を一度に読めません。

### 2.5 物差しが物差しに見えない

ラダーは9段を実寸で並べるため、幅360pxでは3〜4行に折り返します。
順序も量も読めず、「今」ボタンは各段の下の10pxの文字です。
文字サイズの最大段は56pxで、Webのヒーロー見出し(64〜96px)を表せません。

### 2.6 原則との食い違い

| # | 食い違い | 破っている原則 |
|---|---|---|
| 1 | 画像ページで「今」をセットしても、指示文に出ない(`formatNote`が`note.current`を見ていない) | PLAN.md 10-2「すべてのUI操作は、指示文の1行に対応する」 |
| 2 | HTMLの箇所パネルに`background-color: rgba(0, 0, 0, 0)`などの生のCSS名と既定値が並ぶ | PLAN.md 10-6「言葉は会話語」 |
| 3 | 囲っただけの箇所が`### 4 箇所4(x 11%, …)`と中身なしで出力される | 第9章「AIが追加質問なしに作業に入れる」 |
| 4 | スライドを3枚貼ると、箇所が1つの節に混ざり、どのページの話か分からない。ヘッダーも前置きも1枚目しか見ない | 同上 |
| 5 | 取り消しは1段だけで、ラベルを1文字打つと取り消し先が上書きされる。削除は確認なし | 道具としての信頼 |
| 6 | チップを押した直後(フォーカスがボタン)にBackspaceを押すと、選択中の箇所が消える | 同上 |

### 2.7 渡すまでの手数が多い

画像を添付するには、ドロワーを開く→「番号付き画像」タブ→保存→AIに添付→ドロワーに戻る→「指示文」タブ→コピー→貼る、と往復します。

### 2.8 一往復で終わる

デザインのレビューは、初校→赤入れ→再校→照合→校了という往復です。
いまのアプリは「渡す」で終わり、AIが直してきた版を確かめる手段がありません。
直った箇所を次の修正で壊されないようにする手段もありません。

---

## 3. このアプリにおける守破離

| 段階 | 問い | 判定基準 |
|---|---|---|
| **守**(型を守る) | ユーザーがすでに知っている操作が、知っているとおりに動くか。自分たちの原則を実装が守っているか | 慣習(取り消し、前後比較のキー、フォーカス、寸法追従)と[PLAN.md](PLAN.md) 第10章 |
| **破**(型を破る) | その型は、このアプリの目的(なんか違うを分解する)に仕えているか | 仕えていない**借り物の型だけ**を破る。破るときは、代わりに守る原則を必ず書く |
| **離**(型を離れる) | このアプリにしかない型か | 「赤入れ(校正)」の比喩から導けるものだけを採る。校正者は定規と色見本で**測り**、赤字を**書き**、再校で**照合する** |

### 3.1 覆す既存の決定

御用聞きにならないため、過去の計画のうち、今回覆すものを明示します。

| 既存の決定 | 出典 | 今回 | 理由 |
|---|---|---|---|
| 取り消しは直前1段のみ | PLAN.md 第7章 | 何度でも(上限50) | 直接操作の道具で1段は信頼を損なう。ボードは不変データなので、積むだけで済む |
| 画像を貼った直後に「この画像は?」と聞く | PLAN.md 5.3 | 既定は「直したいもの」、あとから切替 | 最初の数秒に判断を挟むと手が止まる。答えは可逆 |
| ラダーは全段を実物で横一列に並べる | PLAN.md 5.6.1 | 目盛り+「今/こうしたい」の2つだけ実寸 | 全段の実寸は折り返し、順序も量も読めない |
| 右パネルに選択肢を置く | PLAN.md 5.2 | 右は指示書、選択肢は箇所の横(Q1) | 目線の往復と53個のフォーム |
| バージョン履歴はやらない | PLAN.md 第3章 | 前回への参照を1本だけ持つ(R2 照合) | 履歴ではなく往復を支える。巨大化しない範囲に留める |

### 3.2 守り続ける決定

| 決定 | 出典 | 今回の扱い |
|---|---|---|
| AIを呼ばない、サーバーなし | PLAN.md 10-5 | 維持。透かし・測定・診断はすべてブラウザ内の決定的な計算 |
| 「今」を推定しない | UI-LANGUAGE.md 第4節 | 維持し、「**今は、測る**」に言い換える(R1)。透かしは「イメージ」と明示し、出力にもルールにも書き込まない |
| 名前だけの選択肢を作らない | PLAN.md 10-3 | 維持。R4の候補にも実物を付ける |
| 数値入力欄を作らない | PLAN.md 10-4 | 維持。定規は「数値を打つ」代わりに「手で当てる」 |
| オンボーディングのツアーを作らない | PLAN-UI.md 第4章 | 維持。見本を開いておく(H6)のはツアーではない |

### 3.3 UI-LANGUAGE.md の破れへの答え

| 破れ | この計画での扱い |
|---|---|
| #2「今」の二重化 | R1で「今の出どころは3つ(実測・測った・申告)」と定義する |
| #5 色の「今」が画面に出ない | S8で、`current`があれば画像の有無に関係なく表示する |
| #6 その場反映がHTMLだけ | H1(透かし)で画像ページにも付ける |
| #9 いま/こうしたいトグル | H1で両ページ共通の「レンズ」にする |
| #10 キー操作の重複 | S6で1か所に集約する |

---

## 4. フェーズ0: 土台を1本にする

**なぜ:** 2.1のとおり、公開版と手元で実装が分岐しています。このままでは、どちらに積んでも片方が失われます。

**やること**

1. `git fetch`
2. `git switch feat/html-page`
3. `git diff fa62a62 origin/main -- docs/DECISIONS.md README.md` を読み、手元にない判断があれば手元の`DECISIONS.md`へ転記する
4. `git merge -s ours origin/main -m "origin/main(PR #1)を統合: 手元のフェーズA〜E実装を正とする"`
   (公開版のA〜D実装は、手元のA〜Eで置き換わるため捨てます。違和感があれば中断し、ユーザーに確認してください)
5. `npm ci`、`npm run build`、`npm test` が通ることを確認する
6. `git switch -c feat/shuhari`
7. **出力の金型を取る。** `test/golden.test.ts`を新設し、`SAMPLES`の2件と、HTMLページの箇所を持つボード1件について、`boardToMarkdown`の結果をスナップショット(`toMatchSnapshot`)で固定する。以降、出力が変わるのは意図した項目(S7・S9・S10・H2・R2)だけで、そのたびにスナップショットを見直して更新する

**受け入れ基準**

- `git merge-base --is-ancestor origin/main HEAD` が成功する
- ビルドとテストが通り、HTML取り込みダイアログにブックマークレットが出る
- 金型テストが通る

---

## 5. 守 — 当たり前を当たり前に

優先度はP0(初見で壊れて見える)、P1(原則違反・信頼)、P2(詰め)です。

### S1. ボードの寸法を実寸に追従させる [P0]

**触るファイル:** 新規`src/lib/useBoxSize.ts`、`src/board/Board.tsx`、`src/board/HtmlBoard.tsx`

- `useBoxSize(ref)`: `ResizeObserver`で要素の`{ width, height }`を返すフック(15行程度)
- 2つのボードで使い、`containRect`はフックの値から1回だけ計算する。`getCr()`とフォールバックの1000×1000は削除する

**受け入れ:** サンプルを開いた瞬間から枠が画像に重なっている。ウィンドウ幅を変えても追従する。

### S2. 縦長のページを幅いっぱいで見せる [P0]

**触るファイル:** `src/styles.css`

- `.board-surface`に`flex-shrink: 0`を足す。これで`aspect-ratio`が保たれ、列が縦にスクロールする
- 画像と枠の比が常に一致するため、S1の`containRect`は実質的に等倍になる

**受け入れ:** Webのサンプルが列の幅いっぱいに出て、縦にスクロールできる。スライドは従来どおり。

### S3. HTMLページの「その場反映」を必ず勝たせる [P0]

**触るファイル:** `src/lib/htmlCss.ts`、`test/htmlCss.test.ts`

- `spotsToCss`の全宣言に`!important`を付ける。反映用の層は常に元ページに勝つべきなので、詳細度の競争に参加させない
- テストの期待値を更新する

**受け入れ:** `#main h1{font-size:56px}`のページで「少し小さく」を選ぶと、見出しが実際に縮む。「いま」に切り替えると元に戻る。

### S4. HTMLページの箇所枠を要素に重ねる [P1]

**触るファイル:** `src/board/HtmlBoard.tsx`

- 統合後に2.2の#4を再確認する。再現すれば、クリック時の比率計算を`el.ownerDocument.documentElement.scrollHeight`(その時点の実測)で割る形にする

**受け入れ:** どの要素をクリックしても、番号枠が要素にぴったり重なる。

### S5. 取り消しを何度でも [P1]

**触るファイル:** `src/state.ts`、新規`src/panels/Toast.tsx`、`src/app.tsx`、新規`test/undo.test.ts`

- `undoSnapshot`を`undoStack`と`redoStack`に置き換える。上限は50(`ponytail:`コメントで上限と理由を明記)
- 直前の積み込みから500ms以内の更新は、同じ1段にまとめる。ラベルの打鍵が1文字ずつ積まれないようにするため
- Ctrl+Zで取り消し、Ctrl+Shift+ZとCtrl+Yでやり直し
- 箇所を削除したとき、画面下に「箇所3を削除しました [元に戻す]」を5秒出す(`toast` signal 1つ)。ボード自体の削除は画像を失うため、従来どおり`confirm`を残す

**受け入れ:** 3つの操作をCtrl+Zで3回戻せる。ラベルを打ってCtrl+Zを押すと、ラベル全体が1回で戻る。箇所の削除でトーストが出て、そこから戻せる。

### S6. キー操作を1か所に、誤爆をなくす [P1]

**触るファイル:** 新規`src/lib/useBoardKeys.ts`、`src/board/Board.tsx`、`src/board/HtmlBoard.tsx`

- 2つのボードに重複しているキー処理を1つのフックに移す
- Delete・Backspace・矢印キーは、`document.activeElement`が`body`かボード内のときだけ効かせる
- Escは「開いているピッカー→箇所の選択」の順に1段ずつ閉じる

**受け入れ:** チップを押した直後のBackspaceで箇所が消えない。2つのボードで同じキーが同じように効く。

### S7. 「今」を出力に届ける [P1]

**触るファイル:** `src/export.ts`、`test/export.test.ts`

- ラダーのノートに`current`があれば、「文字サイズ: 24px → 20px(少し小さく)」の形で出す。`delta`は`current`を起点に解決する
- `current`がなければ従来どおり

**受け入れ:** 画像ページで今=24px、「少し小さく」を選ぶと上記の行が出る。

### S8. 画面の言葉を会話語に戻す [P1]

**触るファイル:** `src/panels/Sheet.tsx`(H3で新設する指示書。`SpotPanel.tsx`はH3で消えるため、画面側はH3の中で実装する)、`src/pickers/ColorPicker.tsx`

- HTMLの箇所では、実測値を「文字 56px・太さ 700・色 ■#111111」のように会話語で出す。透明・0px・既定値は出さない。セレクタは小さく残す
- `ColorPicker`の「今の色」欄は、`hasImage`ではなく`current`の有無で出し分ける(破れ#5)

**受け入れ:** 画面にCSSのプロパティ名が出ない。HTMLの箇所でも「今の色」が見える。

### S9. 中身のない箇所を黙って渡さない [P1]

**触るファイル:** `src/export.ts`(先に実装してよい)、`src/panels/Sheet.tsx`(画面側はH3の中で実装する)

- ノートなし・「残す」なし・位置の差分なしの箇所は、指示文に含めない
- 画面では、その箇所のカードを薄い灰色の点線で出し、「まだ『こうしたい』がありません(このままでは渡されません)」と添える

**受け入れ:** 囲っただけの箇所が出力に出ない。画面では未指定だと分かる。

### S10. 複数ページを取り違えない [P1]

**触るファイル:** `src/export.ts`、`test/export.test.ts`

- ページが2枚以上のとき、箇所を「## p.1」「## p.2」の見出しでページごとに分ける
- ヘッダーの画像サイズと前置きは、ボード内に画像ページ・HTMLページがあるかどうかで組み立てる(1枚目だけを見ない)

**受け入れ:** 3枚のスライドで、各箇所がどのページのものか、指示文だけで分かる。

### S11. 支援技術への伝達 [P2]

**触るファイル:** `src/pickers/*.tsx`

- ラダーの相対チップ、動きの「きっかけ」、色の役割チップに`aria-pressed`を付ける
- 段のボタンに`aria-label`(例: 「20px」)を付ける

**受け入れ:** スクリーンリーダーで段とチップの選択状態が読み上げられる。

---

## 6. 破 — 借り物の型を、目的に合わせて破る

### H1. 透かし — こうしたいを、スクショの上で見せる [最優先の破]

**破る型:** 「画像は書き換えられないので、手応えは文字で返す」
**代わりに守る原則:** 実物で見せる(PLAN.md 10-3)。透かしは表示だけの派生物で、出力にもデータにも書かない。

**触るファイル:** 新規`src/lib/ghost.ts`、新規`src/board/Ghost.tsx`、`src/board/Board.tsx`、`src/board/HtmlBoard.tsx`、`src/state.ts`、`src/styles.css`、`src/samples.ts`、新規`test/ghost.test.ts`

**やること**

1. `ghost.ts`(純関数)
   - `ghostStyle(spot, displayScale)`: ノートから見た目の変形を返す
     | ノート | 透かしでの表現 |
     |---|---|
     | 文字サイズ・全体の大きさ | 拡縮(`transform: scale`、原点は左中央)。`current`があれば段の比。なければ相対段の固定値(−2: 0.75、−1: 0.88、+1: 1.14、+2: 1.33)。`ponytail:`で近似であることを明記 |
     | 角丸 | 切り抜きの`border-radius`(1280基準のpx×表示倍率) |
     | 余白 | 朱の半透明の帯(外側の`box-shadow`)で増減を示す |
     | 動き | `motion-*`クラスと速さ |
     | 位置・大きさ(`targetRect`) | 透かしを`targetRect`の位置に置く。縦横比は保ち、歪ませない |
     | 太さ・線・速さ・強さ | 表現しない(文字の行だけで伝える) |
   - `recolor(data, fromHex, toHex, tolerance)`: 今の色に近い画素だけを、差分で目標色へずらす。距離で重み付けし、文字の縁のなじみを残す。**色ノートに`current`があるときだけ**使う(推定しない)
2. `Ghost.tsx`
   - 箇所の矩形を元画像から切り抜いたcanvasを作る。色ノートがあれば`recolor`をかける。ノートと矩形が変わったときだけ作り直す
   - `ghostStyle`で変形し、`targetRect`(なければ`rect`)の位置に重ねる
   - 元の位置には和紙色のベール(不透明度0.8)を掛け、「ここが置き換わる」ことを示す
   - 動きは1.8秒ごとに再生し直す。`prefers-reduced-motion`ではホバー時に1回だけ
   - 右下に小さく「イメージ」と表示する
   - 対象は`imageRole === 'draft'`の画像ページのみ。HTMLページは実物のCSSが当たるため不要
3. レンズ
   - `state.ts`に`lens` signal(`'after' | 'before'`)を置く。`HtmlBoard`の`showCurrent`はこれに置き換える
   - ボード右上に「いま/こうしたい」の切替を**両方のページ種別で**置く
   - スペースキーを押している間だけ「いま」を覗ける(写真編集ソフトの前後比較と同じ型)。入力欄にフォーカスがあるときは無効
4. `samples.ts`: Webの見本の色ノートから`via: 'warm'`を外す(#3266cc→#c94a1dは「暖かく」ではないため)

**受け入れ基準**

- Webの見本を開くと、見出しが4%上へ移って少し縮み、CTAボタンが青から朱に変わってポンと跳ねている
- スペースを押している間だけ元の見た目に戻る
- 色の方向チップだけで`current`がない色ノートには、透かしが出ない
- 動きを減らす設定では、ループ再生が止まる

### H2. 物差しを、物差しにする

**破る型:** 「全段を実物で横一列に並べる」(PLAN.md 5.6.1)
**代わりに守る原則:** 実物で見せる(比べたい2つだけを実寸で)。現在地を起点に動かす(UI-LANGUAGE.md 候補Bの完成形)。

**触るファイル:** `src/pickers/Ladder.tsx`(Propsは変えない。呼び出し側は無修正)、`src/vocab.ts`、`src/export.ts`、`src/styles.css`

**やること**

- 1行の目盛りにする。各段の値を小さく表記し、上に朱の▼(こうしたい)、下に墨の▲(今)を置く。2つの間の隙間がロゴの「ズレ」と同じ意味になる
- 目盛りのクリックで朱▼、目盛りの下の帯のクリックで墨▲が動く。各段の下の「今」ボタン列は廃止する
- HTMLページは`autoNow`で墨▲が自動で付く。R1の「測る」でも墨▲が入る
- 目盛りの下に、「今|こうしたい」の2つだけを実寸でプレビューする(最大高さ64px)。今が不明なら「今: ?」
- 相対チップの言葉を属性ごとの会話語にする(`vocab.ts`に`RELATIVE_WORDS`)。今が分かれば「少し小さく(20px)」と行き先も添える
  | 属性 | 減らす | 増やす |
  |---|---|---|
  | 文字サイズ・全体の大きさ | 小さく | 大きく |
  | 文字の太さ・線の太さ | 細く | 太く |
  | 余白 | 詰める | 広げる |
  | 角丸 | 角ばらせる | 丸く |
  | 動きの速さ | 速く | ゆっくり |
  | 動きの強さ | 控えめに | 強く |
- 出力の相対語も同じ語彙にする(金型を意図して更新する)
- 文字サイズの段の**末尾**に72と96を足す。Webの大見出しを表せるようにするため。末尾への追加なので、保存済みの段番号はずれない
- ←→キーで朱▼を1段動かす(`role="slider"`と`aria-valuetext`、例: 「20px、少し小さく」)

**受け入れ基準**

- 幅360pxで目盛りが折り返さない
- 今とこうしたいが、実寸で左右に並ぶ
- キーボードだけで段を選べる
- 「余白: 少し広げる」のように、属性に合った言葉で指示文に出る

### H3. 右パネルを「指示書」に、選ぶ場所を「箇所の横」に [Q1: 指示書に反転、で確定]

**破る型:** デザインツールのインスペクタ(右に設定フォーム)。このアプリはデザインツールではなく、PLAN.md 10-6で「Inspector」を画面に出さないと決めています。それなのに形だけ借りた結果、目線が往復し、フォームは53個に伸びました。
**代わりに守る原則:** 出力は常に見えているもの(PLAN.md 第2章)。「すべての操作は指示文の1行に対応する」(10-2)を**逆向きにも**成り立たせます。つまり「**指示文のすべての行は、操作できる**」。

**触るファイル:** `src/export.ts`、新規`src/lib/notes.ts`、新規`src/board/Palette.tsx`、新規`src/panels/Sheet.tsx`、`src/app.tsx`、`src/styles.css`。削除: `src/panels/SpotPanel.tsx`、`src/panels/BoardPanel.tsx`、`src/panels/LivePreview.tsx`

**やること**

1. `export.ts`に`boardToLines(board): Line[]`を足す。`Line`は`{ text: string; spotId?: string; noteId?: string; pageId?: string }`。`boardToMarkdown`は`boardToLines`の`text`を連結するだけにする。**金型テストで出力が1文字も変わらないことを確かめてから**次へ進む
2. `notes.ts`: `SpotPanel.tsx`にある更新処理(色の追加・変更、雰囲気、ラダー、動き、ルール参照、ひとこと、ルールへの昇格)を、`(board: Board) => Board`を返す純関数へ移す。パレットと指示書の両方がこれを使う
3. `Palette.tsx`(朱のパレット)
   - 選択中の箇所の右横に浮く1行のバー。入らなければ左、それも無理なら下に出す
   - 並び: [位置][色][文字][大きさ・余白・形][動き][ひとこと][ルール](ルールがあるときだけ)[残す][削除]
   - 押すと、既存のピッカー(`ColorPicker`・`FontPicker`・`Ladder`・`MotionPicker`)がバーの下にポップオーバーで開く。同時に開くのは1つだけ
   - ネイティブの`popover`属性で最上層に出す。スクロール領域や`overflow: hidden`に切られないようにするため
4. `Sheet.tsx`(指示書、右パネル全体)
   - `boardToLines`を箇所ごとのカードに分けて表示する。高さ制限はなく、右パネル全体を使う
   - 行はボタンにする。クリックでその箇所を選び、その行を作ったピッカーを開く。ホバーでボード上の箇所を強調する(`hoverSpotId` signal)。行末の×でそのノートを消す
   - 選択中の箇所のカードだけ、ラベルの入力欄と「残す」を持つ
   - 先頭カードはボード全体。見出し行「# デザイン指示: …」そのものをボード名の入力欄にする。この画像は(H4)、全体のひとこと、見る順もここに置く
   - 変わった行を朱で0.6秒だけ淡く光らせる。何が変わったかを目で追えるようにするため
5. 引き出し線
   - 行または箇所にホバーすると、行の左端から箇所の辺まで朱の細線を引く
   - `main-layout`全面に`pointer-events: none`のSVGを1枚重ね、`getBoundingClientRect`で線を引く
   - 校正紙の「引き出し線」と同じ意味を持たせる

**受け入れ基準**

- 箇所を選ぶと、その横にパレットが出る。どの「こうしたい」も、箇所から1クリックで開ける
- 右パネルに設定フォームがない(選択中カードのラベルと「残す」を除く)
- 指示文の行をクリックすると、その行を作ったピッカーが開く
- 行にホバーすると、引き出し線が箇所を指す
- 指示文が高さ制限なく右パネル全体に出る
- 金型テストが通る(`boardToLines`への切り替えで出力が変わっていない)

### H4. 「この画像は?」を後回しにする

**破る型:** 画像を貼った直後に1つだけ聞く(PLAN.md 5.3)

**触るファイル:** `src/lib/intake.ts`、`src/app.tsx`、ボード上端の切替(H1のレンズの隣)

- 画像を初めて受け取ったボードは`imageRole = 'draft'`で始める
- ボード上端に「直したい/参考」の小さな切替を常時置く
- `ImageRolePrompt`は削除する

**受け入れ:** 貼った直後に、そのままドラッグして囲める。

### H5. 渡すを1つのボタンで

**破る型:** 書き出しダイアログ(タブを開いて、保存して、戻って、コピーする)

**触るファイル:** `src/panels/Sheet.tsx`(最下部)、`src/panels/ExportDrawer.tsx`、`src/app.tsx`

- 主ボタン「AIに渡す」を指示書の最下部に固定する
- 画像を含むボードでは、1回目で番号付き画像(R3採用時は校正紙)をクリップボードへ入れる(`navigator.clipboard.write`+`ClipboardItem`、`image/png`)。ボタンは「② 指示文をコピー」に変わり、「AIの入力欄に画像を貼ったら、もう一度押してください」と添える。2回目で指示文をコピーする
- HTMLページだけのボードは、1回で指示文をコピーする
- 画像のコピーに失敗したら、PNGを保存して指示文をコピーする
- ドロワーはJSONと画像保存のために残し、トップバーのボタン名を「書き出し」に変える

**受け入れ:** 2回のクリックと2回の貼り付けで、画像と指示文の両方がAIに渡る。

### H6. 入口画面を磨く [Q3: 今の入口画面を磨く、で確定]

**方針:** ドロップゾーンが中心の入口は残します。見本を自動で開くことはしません。
そのかわり、入口にいながら「このアプリで何が起きるか」が一目で分かるようにします。

**触るファイル:** `src/panels/Empty.tsx`、`src/styles.css`(H1の`Ghost.tsx`を再利用)

**やること**

- **透かしの実演**: 「まず触ってみる」の横に、Webの見本の上半分を縮小した小窓(幅280px前後)を置く
  - 見本画像と`SAMPLES[0]`の箇所をそのまま使い、H1の`Ghost.tsx`で描く。見出しが縮んで上へ動き、CTAボタンが青から朱に変わってポンと跳ねる
  - 操作は受け付けない(`pointer-events: none`の小窓全体を1つのボタンにする)。クリックすると「サンプル: Webページ」と同じく見本を開く
  - `prefers-reduced-motion`では、透かしを静止した「こうしたい」の状態で見せる
- **手順を円環にする**: `STEPS`を「1 貼る → 2 囲う → 3 選ぶ → 4 渡す → 5 照合」にし、5の後ろに1へ戻る矢印(↺)を添える。5の説明は「直った版を貼って確かめる」(R2)
- **余力があれば**: 入口のロゴを`<img>`からインラインSVGに替え、朱の点線四角が墨の四角の位置からすっとズレて定位置に収まる動きを、表示時に1回だけ(0.6秒)付ける。ロゴの意味(今とこうしたいのズレ)を最初に1度だけ演じるため

**受け入れ基準**

- 入口画面を開くと、何も押さずに小窓の中で透かしが動いている
- 小窓をクリックすると、Webの見本が開く
- 手順が5つの円環で表示される
- 貼り付け・ドロップ・クリックでのファイル選択は従来どおり動く

---

## 7. 離 — 赤入れを、操作原理にする

いまのアプリは、校正者の仕事のうち「赤字を書く」だけを持っています。
離では、残りの2つを加えます。**測る**(定規と色見本)と**照合する**(再校)です。
さらに、赤字の最終形である**校正紙**で渡します。

この章で、[docs/UI-LANGUAGE.md](docs/UI-LANGUAGE.md) の原則を次のように更新します。

| いまの原則 | 更新後 |
|---|---|
| 「今」を推定しない | **今は、測る。** 今の出どころは3つ: 実測(HTML)、測った(スポイト・定規)、申告(墨▲を手で置く)。どれでもなければ「?」のまま |
| (なし) | **一往復で終わらない。** 渡すの先に照合がある |
| すべてのUI操作は指示文の1行に対応する | **指示文のすべての行は操作できる**(H3)を加え、双方向にする |

### R1. 測る — 定規、色の棚卸し、ばらつき診断 [Q2で採用]

#### R1-a. 定規

**触るファイル:** `src/state.ts`(`measureRequest` signal。スポイトの`colorPickRequest`と同じ形)、`src/board/Board.tsx`、`src/pickers/Ladder.tsx`

- 物差し(H2)に「測る」ボタンを置く(画像ページのみ)
- 押すとボードが測定モードになり、ドラッグした長さを墨の寸法線で見せる。両端に短い縦線、中央に「約24px」
- 長さは画像のpxを1280基準のpxに換算する(×1280÷画像幅)
- 文字サイズ: 1文字の高さを測る。高さ÷0.8を文字サイズとみなし、最寄りの段へ丸める。`ponytail:`で係数が近似であること(和文と欧文で字面の高さが違うこと)を明記する
- 余白・線の太さ・角丸: 測った長さを、そのまま最寄りの段へ丸める
- 結果は`note.current`に入り、墨▲が動く。寸法線は2秒残して消える

**受け入れ:** 見本の見出しの1文字を測ると、文字サイズの墨▲が72px付近に付く。指示文に「72px → …」が出る(S7)。

#### R1-b. 色の棚卸し(画像ページ)

**触るファイル:** 新規`src/lib/palette.ts`、`src/panels/Sheet.tsx`(先頭カード)、`src/board/Board.tsx`、新規`test/palette.test.ts`

- `extractPalette(data: Uint8ClampedArray, max = 8): { hex: string; share: number }[]`(純関数)
  - 画像を短辺200pxに縮小してから渡す
  - 各チャンネルの上位4bitで量子化して集計する
  - 近い色(RGB距離24以内)は多い方へまとめる
  - 1%未満を捨て、上位8色を返す
- 先頭カードに「この画面の色」として、色見本と使用率を並べる
- 色見本にホバーすると、その色が使われている画素をボード上で朱に点滅させる(マスクのcanvasを1枚重ねる)
- クリックで「背景/文字/強調/補助の基準にする」を選び、`rules.palette`に入れる

**受け入れ:** 見本で白・黒・青・灰が出る。青にホバーするとCTAボタンが光る。

#### R1-c. ばらつき診断(HTMLページ)

**触るファイル:** 新規`src/lib/audit.ts`、指示書の先頭カード、`src/board/HtmlBoard.tsx`、新規`test/audit.test.ts`

- 本体は純関数`tally(records)`にする。要素ごとの`{ fontSize, color, background, radius }`から、値→件数の表を作る。DOMから記録を集める部分は薄くする(表示中で、文字を持つ要素のみ)
- 先頭カードに「文字サイズ 11種類/文字色 7種類/背景色 4種類/角丸 5種類」と出す。開くと値ごとの件数が並ぶ
- 値にホバーすると、その値を持つ要素をiframe内で朱の点線で囲む(`data-uic-hit`属性と、`#uic-hover`へのCSS)
- 値の横の「これを本文の基準に」などで、`rules.type`・`rules.palette`へ入れる(最寄りの段に丸める)
- ルールがあるときは「ルールで校正する」を出す。ルールの段から外れた要素を、面積の大きい順に最大12個まで箇所にし、それぞれに「ルールに合わせる」ノートを付ける。これで指示文に行が生まれる

**受け入れ:** 取り込んだページで種類の数が出る。値にホバーすると該当要素が囲まれる。「ルールで校正する」で箇所がまとめてでき、「ルール: 本文の文字サイズ(16px)とズレている」が並ぶ。

### R2. 照合 — 直った版を重ねて確かめる [Q2で採用]

**触るファイル:** `src/schema.ts`、`src/state.ts`、`src/lib/intake.ts`、`src/export.ts`、`src/board/Board.tsx`、指示書・パレット、`docs/SPEC.md`

**データ(省略可能なフィールドの追加のみ)**

```ts
// Board
round?: { prevBoardId: string; n: number };   // n=2で再校、3で三校
// Spot
carried?: boolean;          // 前回から持ち越した箇所
check?: 'ok' | 'ng';        // 照合の結果(○直った/×まだ)
```

**やること**

- 入口は2つ
  - 「AIに渡す」を押したあと、ボタンの下に「AIが直したら、直った画像を貼って照合」を出す(ファイル選択、または次の貼り付けを照合として受ける)
  - 箇所のある画像ボードに画像を貼ったときは、従来どおりページとして足し、トーストで「ページとして足しました [照合に切り替える]」を出す。押すと、足したページを取り消して照合に切り替える
- 照合では、新しいボード「{title}(再校)」を作る。ページは新しい画像、箇所は前回をすべて複製する(`carried: true`、比率座標のまま)
- 各箇所に「○直った/×まだ」の2択を置く(パレットと指示書のカードの両方)
  - ○: `keep = true`にする。直ったものを、次の修正で壊させないため
  - ×: ノートを残し、出力に「(前回も指示・未反映)」を添える
- ボード上端に「前回⇄今回」のスライダー(`<input type="range">`)を置き、前回の画像を半透明で重ねる
- 持ち越した箇所がすべて○になり、ほかに指定もないとき、ボードに朱の「校了」の判子を押す(0.6秒。動きを減らす設定では静止)。指示書には「校了。直すところはありません」と出す

**出力**

- ヘッダーの種類を「修正指示(再校。前回の指示のうち未反映のものを含みます)」にする
- ○の箇所は「## 変えないもの」に「(前回修正済み)」付きで出す

**受け入れ:** 見本を同じ画像で照合し、1を○、2を×にする。指示文で1が変えないものに、2に持ち越しの印が付く。スライダーで前回の画像が重なる。すべて○で判子が出る。

### R3. 校正紙 — 画像1枚で伝わる [Q2で採用]

**触るファイル:** `src/export.ts`、`src/panels/ExportDrawer.tsx`、H5の画像コピー

- `renderProofSheet(page, spots, lines)`を足す
  - 元画像の右に余白を足す(幅の45%、最小480px、和紙色)
  - 箇所ごとに「① 見出し」と赤字の行を朱で書く。行は`boardToLines`の該当行から「- 」を除いたもの
  - 箇所の右辺の中央から、赤字の塊へ朱の引き出し線を引く
  - 塊は箇所のy順に積み、重なるときは下へずらす
  - 和文は1文字単位で折り返す(`measureText`)。描く前に`document.fonts.ready`を待つ
  - 余白の末尾に「変えないもの」を書く
- H5の画像コピーと、書き出しドロワーの画像はこれを既定にする。従来の番号付き画像も選べるように残す

**受け入れ:** 書き出した画像だけをAIに貼っても、箇所と指示が読める。赤字の文言が指示文と一致する。

### R4. 言葉で探す — 打てば語彙が出る [Q2で採用]

**触るファイル:** 新規`src/lib/commands.ts`、`src/vocab.ts`(`kana`の追記)、`src/board/Palette.tsx`、新規`test/commands.test.ts`

- 箇所を選んだ状態で文字キーを打つ(または`/`)と、パレットに入力欄が開く
- `commands.ts`(純関数)が`vocab.ts`から全候補を作る。ラダー×相対語、色の方向、雰囲気、動き、ひとこと、位置、残す
- 表記と`kana`(ひらがな読み)の両方で部分一致させる。IMEで変換前のひらがなでも当たるようにするため
- 候補には実物を付ける(色見本・書体・動き)。Enterで適用する

**受け入れ:** 箇所を選び、「ちいさ」と打ってEnterで「文字サイズ: 少し小さく」の行が増える。「ふわ」で「下からふわっと」が候補に出る。

---

## 8. 決定事項(ユーザー回答、2026-09-11)

| # | 問い | 回答 | 反映先 |
|---|---|---|---|
| Q1 | 右パネルの役割 | **指示書に反転する**(右は指示書、選ぶのは箇所の横の朱のパレット) | 6章 H3 |
| Q2 | 離の範囲 | **4つすべて**(測る・照合・校正紙・言葉で探す) | 7章 R1〜R4 |
| Q3 | 初回体験 | **今の入口画面を磨く**(見本を自動で開かない) | 6章 H6 |
| 前提 | 土台 | 手元の`feat/html-page`を正とする(ユーザーから異論なし) | 4章 |

本文はすべてこの回答に合わせて書いてあります。

---

## 9. やらないこと

| やらないこと | 理由 |
|---|---|
| AI・LLM API・サーバー・アカウント | PLAN.md 第3章。透かし・測定・診断・照合は、すべてブラウザ内の決定的な計算で行う |
| 新しい本番依存パッケージ | preactと標準API(ResizeObserver、Canvas、Clipboard、Popover)で足りる |
| OCR(文字認識)、手書きの校正記号の認識 | 精度の責任を負えない。定規は「人の手で当てる」ことに価値がある |
| ルールドロワーの作り直し | 今回はR1で「現物からルールを起こす」経路を足すに留める |
| バージョン履歴(3版以上の一覧・差分) | R2の「前回への参照1本」に限定する |
| CSS差分の書き出しタブ | PLAN-HTML.md 第5章の判断を維持する |
| ダークモード、モバイル対応 | PLAN.md 第3章の判断を維持する |
| 透かしを出力・データに書き込む | 表示だけの派生物にする。「今を推定しない」を崩さないため |

---

## 10. 検証

各項目の完了時に実行してください。

```bash
npm run build
```

```bash
npm test
```

`npm run dev`で、次の順に目視確認します。

1. 入口画面で、何も押さずに小窓の中の透かしが動いている。小窓のクリックで見本が開く
2. ウィンドウ幅を変えても、枠が画像に重なったまま
3. 見出しの箇所で「少し小さく」→スクショ上の見出しが縮み、指示文の行が朱に光る
4. スペースを押している間だけ、元の見た目に戻る
5. HTMLを取り込み(`#main h1`を持つページ)、見出しを縮める→実際に縮む
6. 箇所を消す→トーストの「元に戻す」で戻る。Ctrl+Zを3回→3操作が戻る
7. 「AIに渡す」を2回押す→画像と指示文がそれぞれクリップボードに入る
8. 箇所を選ぶと横にパレットが出て、指示書の行をクリックすると該当ピッカーが開く。行にホバーすると引き出し線が出る
9. R1〜R4の受け入れ基準をすべて通す

実装がすべて終わったら、`docs/screenshot.png`に実機のスクリーンショットを保存し、README冒頭の画像を復活させてください(PLAN-UI.md 第5章からの宿題です)。
あわせて、README(使い方の手順)、[docs/SPEC.md](docs/SPEC.md)(追加フィールドと出力の変化)、[docs/UI-LANGUAGE.md](docs/UI-LANGUAGE.md)(第7章の原則の更新)を直してください。

---

## 11. 順序と優先度

| 順 | 項目 | 効果 | 規模 |
|---|---|---|---|
| 1 | フェーズ0(土台の統一と出力の金型) | 前提 | 小 |
| 2 | S1〜S3(P0) | 大。初見で壊れて見えるものがなくなる | 小 |
| 3 | H1 透かし | **特大。驚きの芯** | 中 |
| 4 | S5〜S7、S10、S9の出力側 | 大。信頼と出力の正しさ | 中 |
| 5 | H2 物差し | 大 | 中 |
| 6 | H3 指示書(S8とS9の画面側を含む) | 特大。目線が箇所から離れなくなる | 大 |
| 7 | H4・H5・H6 | 中〜大 | 小〜中 |
| 8 | R1 測る | 大 | 中 |
| 9 | R2 照合 | 特大。一往復で終わらなくなる | 中 |
| 10 | R3 校正紙 | 中 | 中 |
| 11 | R4 言葉で探す | 中 | 小〜中 |
| 12 | S4・S11(P2)、ドキュメント更新 | 小 | 小 |

- S8とS9の画面側は、H3の中で実装してください。`SpotPanel.tsx`と`LivePreview.tsx`はH3で消えるため、先に直すと二度手間になります
- コミットは項目ごとに分けてください(1行目は日本語の要約)
- 時間が足りない場合は、**1〜3だけでも体験は大きく変わります**。その次に効くのは6と9です
